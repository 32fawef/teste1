-- ============================================================
-- ZURION PRO v8 - SILENT AIM & FOV EDITION
-- A câmera NÃO trava. O alvo é selecionado pelo círculo.
-- ============================================================

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local Mouse = LocalPlayer:GetMouse()

-- Configurações
local Settings = {
    Aimbot = false,
    FOV = 150, -- Tamanho do círculo
    Smoothness = 0.1, -- Suavidade (0.1 a 1)
    ShowFOV = true
}

-- Criar Círculo de FOV (Visual)
local FOVCircle = Drawing.new("Circle")
FOVCircle.Thickness = 2
FOVCircle.Color = Color3.fromRGB(255, 0, 0)
FOVCircle.Filled = false
FOVCircle.Transparency = 1
FOVCircle.Visible = Settings.ShowFOV

-- Interface (Compacta)
local ScreenGui = Instance.new("ScreenGui", game:GetService("CoreGui") or LocalPlayer.PlayerGui)
local Main = Instance.new("Frame", ScreenGui)
Main.Size = UDim2.new(0, 180, 0, 120)
Main.Position = UDim2.new(0.05, 0, 0.4, 0)
Main.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
Main.Active = true
Main.Draggable = true

local Title = Instance.new("TextLabel", Main)
Title.Size = UDim2.new(1, 0, 0, 30)
Title.Text = "ZURION V8 SILENT"
Title.BackgroundColor3 = Color3.fromRGB(160, 0, 0)
Title.TextColor3 = Color3.new(1,1,1)

local AimBtn = Instance.new("TextButton", Main)
AimBtn.Size = UDim2.new(0.9, 0, 0, 35)
AimBtn.Position = UDim2.new(0.05, 0, 0.35, 0)
AimBtn.Text = "AIMBOT: OFF"
AimBtn.BackgroundColor3 = Color3.fromRGB(100, 0, 0)
AimBtn.TextColor3 = Color3.new(1,1,1)

local EspBtn = Instance.new("TextButton", Main)
EspBtn.Size = UDim2.new(0.9, 0, 0, 35)
EspBtn.Position = UDim2.new(0.05, 0, 0.7, 0)
EspBtn.Text = "ESP: OFF"
EspBtn.BackgroundColor3 = Color3.fromRGB(100, 0, 0)
EspBtn.TextColor3 = Color3.new(1,1,1)

local _ESP = false

-- Alternar Estados
AimBtn.MouseButton1Click:Connect(function()
    Settings.Aimbot = not Settings.Aimbot
    AimBtn.Text = Settings.Aimbot and "AIMBOT: ON" or "AIMBOT: OFF"
    AimBtn.BackgroundColor3 = Settings.Aimbot and Color3.fromRGB(0, 120, 0) or Color3.fromRGB(100, 0, 0)
end)

EspBtn.MouseButton1Click:Connect(function()
    _ESP = not _ESP
    EspBtn.Text = _ESP and "ESP: ON" or "ESP: OFF"
    EspBtn.BackgroundColor3 = _ESP and Color3.fromRGB(0, 120, 0) or Color3.fromRGB(100, 0, 0)
end)

-- Função para achar alvo dentro do Círculo
local function GetClosestInFOV()
    local target = nil
    local dist = Settings.FOV

    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
            local hum = p.Character:FindFirstChild("Humanoid")
            if hum and hum.Health > 0 then
                local pos, onScreen = Camera:WorldToViewportPoint(p.Character.HumanoidRootPart.Position)
                if onScreen then
                    local mouseDist = (Vector2.new(pos.X, pos.Y) - Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)).Magnitude
                    if mouseDist < dist then
                        dist = mouseDist
                        target = p
                    end
                end
            end
        end
    end
    return target
end

-- Loop de Renderização
RunService.RenderStepped:Connect(function()
    -- Atualizar Círculo
    FOVCircle.Position = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
    FOVCircle.Radius = Settings.FOV
    FOVCircle.Visible = Settings.Aimbot

    -- Aimbot Suave (Não trava a câmera, apenas ajuda a focar)
    if Settings.Aimbot then
        local target = GetClosestInFOV()
        if target and target.Character:FindFirstChild("HumanoidRootPart") then
            local targetPos = Camera:WorldToViewportPoint(target.Character.HumanoidRootPart.Position)
            local mousePos = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)
            
            -- Move a câmera sutilmente em direção ao alvo se ele estiver no FOV
            local moveX = (targetPos.X - mousePos.X) * Settings.Smoothness
            local moveY = (targetPos.Y - mousePos.Y) * Settings.Smoothness
            
            -- Em mobile, usamos isso para rotacionar levemente
            if targetPos then
                Camera.CFrame = Camera.CFrame:Lerp(CFrame.new(Camera.CFrame.Position, target.Character.HumanoidRootPart.Position), Settings.Smoothness)
            end
        end
    end

    -- ESP Simples (Box)
    if _ESP then
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                local box = p.Character:FindFirstChild("SelectionBox")
                if not box then
                    box = Instance.new("SelectionBox", p.Character)
                    box.Adornee = p.Character
                    box.AlwaysOnTop = true
                    box.Color3 = Color3.new(1, 0, 0)
                end
            end
        end
    end
end)
